+++
title = "WF66 — a token-IR optimizing Forth"
date = 2026-06-10
description = "The successor to WF65: keep the proven STC Forth runtime, but replace the compiler — capture each definition as a token IR and optimize the tokens as data before emitting code."
[taxonomies]
tags = ["forth", "compiler", "jit", "rust", "token-ir"]
[extra]
repo = "https://github.com/albanread/WF66"
language = "Rust (over the JASM/Rasm encoder)"
platform = "64-bit (STC Forth)"
status = "Working"
period = "2026-06"
downloads = []
+++

_WF65 optimized by emitting code and then rewriting it. WF66 optimizes the tokens
first, as data, and only then emits._

## TL;DR

- **What:** a token-IR optimizing **Forth**, successor to WF65 (a complete,
  working 64-bit STC Forth JIT-compiled via [JASM](/posts/jasm)).
- **The change in one line:** WF65 emits eagerly then *replays* (rewind +
  rewrite) to optimize; **WF66 captures each definition as a token IR, optimizes
  the tokens as data, and only then emits.**
- **Reuse:** keeps WF65's proven substrate — JASM/Rasm native encoding, the STC
  kernel primitives, the dictionary, the live REPL, the Rust test harness.
- **Get it:** [Downloads](#download-run) · [Source](https://github.com/albanread/WF66)

## Where it sits

The Forth lineage runs WF32 STC (Alex McDonald's Win32 Forth) →
[WF64](/posts/wf64) → [WF65](/posts/wf65) → **WF66** → [MF66](/posts/mf66) →
[MF67](/posts/mf67). WF66 is the compiler-architecture turning point that MF66
then carried onto Apple Silicon. See the [timeline](/timeline).

## What it is

> _From the README —_ "WF66 is the successor to **WF65**… It reuses WF65's proven
> runtime substrate… and **replaces the compiler**. WF66 captures each definition
> as a **token IR, optimizes the tokens as data**, and only then emits."

## Why I built it

- _Rewriting emitted machine code is fragile; optimizing an IR is not._
- _Keeping a known-good runtime while swapping the brain of the compiler._

## How it works

- **Token IR:** _how a definition becomes a token stream, what optimizations run
  over it (constant folding, stack-effect reasoning, peephole on tokens)._
- **Emit:** _tokens → JASM/Rasm native encoding → executable memory._
- **Inherited substrate:** STC kernel primitives, dictionary, live REPL.

## What works today

> _Fill from `bench/`, `tests/`, `examples/`. Compare against WF65 to show the
> optimizer's payoff._

## Screenshots

The WF66 Forth IDE — the whole cooperative-agent workspace in one frame: a live
**`forth REPL`** (`100 200 + .` → `300 ok`, then defining and running a `t2` loop),
the **`fedit`** editor on `gfx-julia.f`, a searchable **Manual** pane, and two live
graphics windows — a Julia set and a shapes demo. The editor is open on the demo's
**pane-agent**: it fills a row with `gpane-fill-rect`, then `pause`s to yield so the
console stays live — WF66's cooperative multitasking, written in Forth.

![The WF66 Forth IDE: REPL, editor on gfx-julia.f, the manual pane, and live Julia + shapes windows](/images/wf66/wf66-ide.png)

> _(The IDE binary still reports itself as **WF64 0.3.0** — the rename to WF66 is
> deferred follow-up work; the folder and the demo path in this shot are WF66.)_

## Download & run

Prebuilt binaries: the [GitHub Releases page](https://github.com/albanread/WF66/releases).

```bash
git clone https://github.com/albanread/WF66
cd WF66
cargo build --release
```

## Notes, dead-ends, lessons

- _"Optimize as data, then emit" vs. "emit, then rewrite" — why the IR won._

## Links

- Source: https://github.com/albanread/WF66
- Built on: [JASM](/posts/jasm)
- Successor: [MF66](/posts/mf66)
